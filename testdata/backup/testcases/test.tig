let
    function sum(v1: int, v2: int, v3: int, v4: int, v5: int, v6: int, v7: int) : int =
        let
            var sum := v1 + v2 + v3 + v4 + v5 + v6 + v7
        in
            sum
        end
in
    printi(sum(1, 2, 3, 4, 5, 6, 7))
end